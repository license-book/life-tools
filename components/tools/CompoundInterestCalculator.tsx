"use client";
import { useMemo,useState } from "react";
const won=(v:number)=>Math.round(v).toLocaleString("ko-KR")+"원";
export default function CompoundInterestCalculator(){const [principal,setPrincipal]=useState(10000000);const [rate,setRate]=useState(5);const [years,setYears]=useState(10);const [times,setTimes]=useState(12);const r=useMemo(()=>{const total=principal*Math.pow(1+(rate/100)/times,times*years);return{total,interest:total-principal}},[principal,rate,years,times]);return <div className="tool-layout"><section className="panel"><div className="field"><label>원금</label><input type="number" value={principal} onChange={e=>setPrincipal(Number(e.target.value))}/></div><div className="field"><label>연 수익률(%)</label><input type="number" step="0.1" value={rate} onChange={e=>setRate(Number(e.target.value))}/></div><div className="field"><label>기간(년)</label><input type="number" min="1" value={years} onChange={e=>setYears(Number(e.target.value))}/></div><div className="field"><label>연 복리 횟수</label><select value={times} onChange={e=>setTimes(Number(e.target.value))}><option value={1}>연 1회</option><option value={4}>분기</option><option value={12}>월</option></select></div></section><section className="panel"><div className="result-main">최종금액 {won(r.total)}</div><div className="stats"><div className="stat"><small>원금</small><strong>{won(principal)}</strong></div><div className="stat"><small>누적 수익</small><strong>{won(r.interest)}</strong></div><div className="stat"><small>기간</small><strong>{years}년</strong></div><div className="stat"><small>연 수익률</small><strong>{rate}%</strong></div></div></section></div>