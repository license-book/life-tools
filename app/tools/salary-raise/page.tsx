import type {Metadata} from "next";import {SalaryRaiseCalculator} from "@/components/tools/WorkEmployeeCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {salaryRaiseTool} from "@/data/tools/work-employee";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(salaryRaiseTool);export default function Page(){return <ToolPageRenderer tool={salaryRaiseTool} calculator={<SalaryRaiseCalculator/>}/>;}