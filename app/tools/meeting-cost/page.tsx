import type {Metadata} from "next";import {MeetingCostCalculator} from "@/components/tools/WorkCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {meetingCostTool} from "@/data/tools/work-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(meetingCostTool);export default function Page(){return <ToolPageRenderer tool={meetingCostTool} calculator={<MeetingCostCalculator/>}/>;}