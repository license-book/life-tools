"use client";
import { useMemo,useState } from "react";
const won=(v:number)=>Math.round(v).toLocaleString("ko-KR")+"원";
export default function SalaryNetCalculator(){const [gross,setGross]=useState(3500000);const [nonTax,setNonTax]=useState(200000);const [incomeTax,setIncomeTax]=useState(70000);const r=useMemo(()=>{const base=Math.max(0,gross-nonTax);const pension=base*0.0475;const healthLong=base*0.040674;const employment=base*0.009;const localTax=incomeTax*0.1;const deductions=pension+healthLong+employment+incomeTax+localTax;return{base,pension,healthLong,employment,localTax,deductions,net:gross-deductions}},[gross,nonTax,incomeTax]);return <div className="tool-layout"><section className="panel"><div className="field"><label>월 급여(세전)</label><input type="number" value={gross} onChange={e=>setGross(Number(e.target.value))}/></div><div className="field"><label>비과세 금액</label><input type="number" value={nonTax} onChange={e=>setNonTax(Number(e.target.value))}/></div><div className="field"><label>월 소득세 원천징수액</label><input type="number" value={incomeTax} onChange={e=>setIncomeTax(Number(e.target.value))}/><small className="field-help">국세청 근로소득 간이세액표 또는 급여명세서의 소득세 금액을 입력하세요.</small></div></section><section className="panel"><div className="result-main">예상 실수령액 {won(r.net)}</div><div className="stats"><div className="stat"><small>국민연금(4.75%)</small><strong>{won(r.pension)}</strong></div><div className="stat"><small>건강+장기요양(4.0674%)</small><strong>{won(r.healthLong)}</strong></div><div className="stat"><small>고용보험(0.9%)</small><strong>{won(r.employment)}</strong></div><div className="stat"><small>소득세+지방소득세</small><strong>{won(incomeTax+r.localTax)}</strong></div><div className="stat"><small>총 공제</small><strong>{won(r.deductions)}</strong></div><div className="stat"><small>공제 기준금액</small><strong>{won(r.base)}</strong></div></div></section></div>;
}