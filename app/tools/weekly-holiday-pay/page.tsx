import type {Metadata} from "next";import {WeeklyHolidayPayCalculator} from "@/components/tools/WorkCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {weeklyHolidayPayTool} from "@/data/tools/work-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(weeklyHolidayPayTool);export default function Page(){return <ToolPageRenderer tool={weeklyHolidayPayTool} calculator={<WeeklyHolidayPayCalculator/>}/>;}