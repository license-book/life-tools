import type {Metadata} from "next";import {CouponStackCalculator} from "@/components/tools/BuyCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {couponStackTool} from "@/data/tools/buy-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(couponStackTool);export default function Page(){return <ToolPageRenderer tool={couponStackTool} calculator={<CouponStackCalculator/>}/>;}