import type {Metadata} from "next";import {WorkHoursCalculator} from "@/components/tools/WorkCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {workHoursTool} from "@/data/tools/work-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(workHoursTool);export default function Page(){return <ToolPageRenderer tool={workHoursTool} calculator={<WorkHoursCalculator/>}/>;}