import type {Metadata} from "next";import {OvertimePayCalculator} from "@/components/tools/WorkCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {overtimePayTool} from "@/data/tools/work-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(overtimePayTool);export default function Page(){return <ToolPageRenderer tool={overtimePayTool} calculator={<OvertimePayCalculator/>}/>;}