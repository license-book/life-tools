import type {Metadata} from "next";import {DateDurationCalculator} from "@/components/tools/LifeCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {dateDurationTool} from "@/data/tools/life-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(dateDurationTool);export default function Page(){return <ToolPageRenderer tool={dateDurationTool} calculator={<DateDurationCalculator/>}/>;}