import type {Metadata} from "next";import {TripFuelCostCalculator} from "@/components/tools/CarCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {tripFuelCostTool} from "@/data/tools/car-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(tripFuelCostTool);export default function Page(){return <ToolPageRenderer tool={tripFuelCostTool} calculator={<TripFuelCostCalculator/>}/>;}