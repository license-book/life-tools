import type {Metadata} from "next";import {TravelChecklistCalculator} from "@/components/tools/LifeCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {travelChecklistTool} from "@/data/tools/life-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(travelChecklistTool);export default function Page(){return <ToolPageRenderer tool={travelChecklistTool} calculator={<TravelChecklistCalculator/>}/>;}