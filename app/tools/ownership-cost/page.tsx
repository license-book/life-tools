import type {Metadata} from "next";import {OwnershipCostCalculator} from "@/components/tools/CarCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {ownershipCostTool} from "@/data/tools/car-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(ownershipCostTool);export default function Page(){return <ToolPageRenderer tool={ownershipCostTool} calculator={<OwnershipCostCalculator/>}/>;}