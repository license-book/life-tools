import type {Metadata} from "next";import {DeadlineBusinessDayCalculator} from "@/components/tools/WorkCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {deadlineBusinessDayTool} from "@/data/tools/work-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(deadlineBusinessDayTool);export default function Page(){return <ToolPageRenderer tool={deadlineBusinessDayTool} calculator={<DeadlineBusinessDayCalculator/>}/>;}