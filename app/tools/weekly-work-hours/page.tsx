import type {Metadata} from "next";import {WeeklyHoursCalculator} from "@/components/tools/WorkCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {weeklyWorkHoursTool} from "@/data/tools/work-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(weeklyWorkHoursTool);export default function Page(){return <ToolPageRenderer tool={weeklyWorkHoursTool} calculator={<WeeklyHoursCalculator/>}/>;}