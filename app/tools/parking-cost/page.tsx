import type {Metadata} from "next";import {ParkingCostCalculator} from "@/components/tools/CarCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {parkingCostTool} from "@/data/tools/car-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(parkingCostTool);export default function Page(){return <ToolPageRenderer tool={parkingCostTool} calculator={<ParkingCostCalculator/>}/>;}