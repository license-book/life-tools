import type {Metadata} from "next";import {TenureDurationCalculator} from "@/components/tools/WorkCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {tenureDurationTool} from "@/data/tools/work-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(tenureDurationTool);export default function Page(){return <ToolPageRenderer tool={tenureDurationTool} calculator={<TenureDurationCalculator/>}/>;}