import type {Metadata} from "next";import {TimeDurationCalculator} from "@/components/tools/LifeMoreCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {timeDurationTool} from "@/data/tools/life-more";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(timeDurationTool);export default function Page(){return <ToolPageRenderer tool={timeDurationTool} calculator={<TimeDurationCalculator/>}/>;}