import type {Metadata} from "next";import {DepreciationCalculator} from "@/components/tools/CarCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {depreciationTool} from "@/data/tools/car-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(depreciationTool);export default function Page(){return <ToolPageRenderer tool={depreciationTool} calculator={<DepreciationCalculator/>}/>;}