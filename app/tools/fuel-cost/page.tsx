import type {Metadata} from "next";import {FuelCostCalculator} from "@/components/tools/CarCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {fuelCostTool} from "@/data/tools/car-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(fuelCostTool);export default function Page(){return <ToolPageRenderer tool={fuelCostTool} calculator={<FuelCostCalculator/>}/>;}