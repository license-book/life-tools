"use client";
import { useMemo, useState } from "react";
const won=(v:number)=>Math.round(v).toLocaleString("ko-KR")+"원";
export default function DepositInterestCalculator(){const [principal,setPrincipal]=useState(10000000);const [rate,setRate]=useState(3.5);const [months,setMonths]=useState(12);const [tax,setTax]=useState(15.4);const r=useMemo(()=>{const gross=principal*(rate/100)*(months/12);const taxAmount=gross*(tax/100);return{gross,taxAmount,net:gross-taxAmount,total:principal+gross-taxAmount}},[principal,rate,months,tax]);return <div className="tool-layout"><section className="panel"><div className="field"><label>예치금액</label><input type="number" value={principal} onChange={e=>setPrincipal(Number(e.target.value))}/></div><div className="field"><label>연 이자율(%)</label><input type="number" step="0.1" value={rate} onChange={e=>setRate(Number(e.target.value))}/></div><div className="field"><label>예치기간(개월)</label><input type="number" min="1" value={months} onChange={e=>setMonths(Number(e.target.value))}/></div><div className="field"><label>이자소득세율(%)</label><input type="number" step="0.1" value={tax} onChange={e=>setTax(Number(e.target.value))}/></div></section><section className="panel"><div className="result-main">세후 이자 {won(r.net)}</div><div className="stats"><div className="stat"><small>세전 이자</small><strong>{won(r.gross)}</strong></div><div className="stat"><small>세금</small><strong>{won(r.taxAmount)}</strong></div><div className="stat"><small>만기 수령액</small><strong>{won(r.total)}</strong></div><div className="stat"><small>적용 세율</small><strong>{tax}%</strong></div></div></section></div>;
}