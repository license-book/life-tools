import type {Metadata} from "next";import {DepartureTimeCalculator} from "@/components/tools/LifeMoreCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {departureTimeTool} from "@/data/tools/life-more";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(departureTimeTool);export default function Page(){return <ToolPageRenderer tool={departureTimeTool} calculator={<DepartureTimeCalculator/>}/>;}