import type {Metadata} from "next";import {TileQuantityCalculator} from "@/components/tools/HomeCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {tileQuantityTool} from "@/data/tools/home-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(tileQuantityTool);export default function Page(){return <ToolPageRenderer tool={tileQuantityTool} calculator={<TileQuantityCalculator/>}/>;}