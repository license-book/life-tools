"use client";
import { useMemo,useState } from "react";
const won=(v:number)=>Math.round(v).toLocaleString("ko-KR")+"원";
export default function DiscountCalculator(){const [price,setPrice]=useState(100000);const [discount,setDiscount]=useState(20);const r=useMemo(()=>{const saved=price*(discount/100);return{saved,final:price-saved}},[price,discount]);return <div className="tool-layout"><section className="panel"><div className="field"><label>정가</label><input type="number" value={price} onChange={e=>setPrice(Number(e.target.value))}/></div><div className="field"><label>할인율(%)</label><input type="number" min="0" max="100" step="1" value={discount} onChange={e=>setDiscount(Number(e.target.value))}/></div></section><section className="panel"><div className="result-main">할인가 {won(r.final)}</div><div className="stats"><div className="stat"><small>정가</small><strong>{won(price)}</strong></div><div className="stat"><small>할인금액</small><strong>{won(r.saved)}</strong></div><div className="stat"><small>할인율</small><strong>{discount}%</strong></div><div className="stat"><small>최종금액</small><strong>{won(r.final)}</strong></div></div></section></div>;
}