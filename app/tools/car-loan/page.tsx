import type {Metadata} from "next";import {CarLoanCalculator} from "@/components/tools/CarCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {carLoanTool} from "@/data/tools/car-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(carLoanTool);export default function Page(){return <ToolPageRenderer tool={carLoanTool} calculator={<CarLoanCalculator/>}/>;}