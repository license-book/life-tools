import type {Metadata} from "next";import {MovingBoxCalculator} from "@/components/tools/HomeCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {movingBoxTool} from "@/data/tools/home-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(movingBoxTool);export default function Page(){return <ToolPageRenderer tool={movingBoxTool} calculator={<MovingBoxCalculator/>}/>;}