import type {Metadata} from "next";import {RoomAreaCalculator} from "@/components/tools/HomeCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {roomAreaTool} from "@/data/tools/home-basics";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(roomAreaTool);export default function Page(){return <ToolPageRenderer tool={roomAreaTool} calculator={<RoomAreaCalculator/>}/>;}