import type {Metadata} from "next";import {WeeklyPayCalculator} from "@/components/tools/WorkEmployeeCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {weeklyPayTool} from "@/data/tools/work-employee";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(weeklyPayTool);export default function Page(){return <ToolPageRenderer tool={weeklyPayTool} calculator={<WeeklyPayCalculator/>}/>;}