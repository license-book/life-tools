import type {Metadata} from "next";import {PaceCalculator} from "@/components/tools/LifeMoreCalculators";import ToolPageRenderer from "@/components/tools/ToolPageRenderer";import {paceCalculatorTool} from "@/data/tools/life-more";import {createToolMetadata} from "@/lib/tools/engine";export const metadata:Metadata=createToolMetadata(paceCalculatorTool);export default function Page(){return <ToolPageRenderer tool={paceCalculatorTool} calculator={<PaceCalculator/>}/>;}